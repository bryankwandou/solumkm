export const AUTH_TOKEN_KEY = "auth_token";
export const AUTH_COOKIE_NAME = "auth_token";

// Shared judging account. Its sample transactions are read-only so that one
// reviewer cannot delete the demo data and leave the next reviewer with an
// empty account. New transactions can still be added; only deletion is blocked.
export const DEMO_ACCOUNT_EMAIL = "juri@solumkm.app";
